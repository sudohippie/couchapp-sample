/**
 * Raghav Sidhanti
 * 2/5/15
 */
define([], function () {
    'use strict';

    return {
        eventTable: require(['text!template/eventTable.html']),
        eventTableRow: require(['text!template/eventTableRow.html'])
    };
});
